This is the (B-Z)users profile page, that when  user-A goes too via their own welcome page and
clicks on the (B-Z)users name

MVP

it will have the name of the user
it will show only that users 'hiss'(tweets)
it will have a logout button

Swag_VP

*it will have the users avatar picquit
